# Cloudflare credentials (optional - only needed for `wrangler deploy` to CF)
# Leave blank to run in local mode via Docker
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=

# Port exposed by the container (default: 8787)
PORT=8787
